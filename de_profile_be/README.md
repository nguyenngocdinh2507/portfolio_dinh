# CV Studio BE

Backend Node thuần cho CV Studio. API đọc/ghi dữ liệu CV trong `data/cv.json`.

## Scripts

```bash
npm run dev
npm start
```

Server mặc định chạy tại `http://localhost:4000`.

## API

```text
GET /api/health
GET /api/cv
PUT /api/cv
```
